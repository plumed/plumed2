FROM plumed:fedora39

RUN source /etc/bashrc \
 && module load mpi \
 && cd plumed2 \
 && source ./sourceme.sh \
 && cd plugins/pycv \
 && ln -s $(realpath ../../regtest/scripts) ./regtest/scripts \
 && make check
