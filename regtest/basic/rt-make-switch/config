type=make

function plumed_regtest_after() {
  #zfix add a space when removing the minus sign, this should address that
  for file in *.reference; do
    new="${file%.reference}"
    if test -f "$new"; then
      sed -e "s/\( 0.0\+\w\)/ \1/g" -i "$new"
    fi
  done
}
