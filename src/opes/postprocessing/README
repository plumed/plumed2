work in progress...

the idea is to eventually build a tool similar to plumed sum_hills, for printing the running FES estimate

for reweighting instead one can use the plumed REWEIGHT_BIAS, or any program that makes weighted KDE/histograms
