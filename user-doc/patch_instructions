From this directory you will then need to issue the command:</p> <div style="width: 100%; float:left">Click on the options in the command shown below for more information</div> <pre class="fragment"> plumed-patch <b onclick='openModal("patch-options")'>--runtime</b> </pre> <p>You then need to select @MDcode@ from list of codes that are output.  Once this script has completed you then proceed to compile @MDcode@ as you would normally do.</p>
